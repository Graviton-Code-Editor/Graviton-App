import styled from "styled-components";

const LogoIcon = styled.img`
    width: 20px;
    height: 20px;
    position: absolute;
    left: 8px;
    top: 7px;
    user-selects: none;
`;

export default LogoIcon;
