`<h1 style="font-size:5vh; line-height:3em; text-align:center; position:relative; " class="translate_word"
  idT="Languages">${
  getTranslation("Languages")
  }</h1>
<div id='language_list' class=welcome>
  <elastic-container related=parent></elastic-container>
</div>
<button onclick=" themes.length !=0 ? Setup.navigate('themes') : Setup.navigate('additional_settings') "
  style=" position:fixed; right:5%; bottom: 5%;" class="button1 translate_word" idT="Continue">${
  getTranslation("Continue")
  }</button>`